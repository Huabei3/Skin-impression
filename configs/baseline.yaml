# Baseline: 原始MS-CAN（所有模块关闭）
model_name: 'MSCAN_baseline'
modules:
  skin_region_attention:
    enabled: false
  color_space_branches:
    enabled: false
  skin_statistics:
    enabled: false
  multi_task:
    enabled: false
training:
  batch_size: 32
  epochs: 100
  lr: 1e-4
